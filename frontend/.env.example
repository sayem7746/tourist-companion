# Public client configuration only. Never put API keys, JWT secrets, or DB URLs here.
# Angular file replacements live in src/environments/. Copy a named example if you
# use a local dotenv tool; CI injects NG_APP_API_BASE_URL via GitHub Actions (see infra/SECRETS.md).

NG_APP_API_BASE_URL=http://localhost:3000
